# Copy to .env (same directory as docker-compose.yml) to fill in optional
# values for `docker compose up`. All optional — the app runs fine with
# these left blank; you just won't get AI insights or login.
OPENAI_API_KEY=
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
