fastapi==0.115.0
uvicorn[standard]==0.32.0
pandas==2.2.3
numpy==2.1.3
scikit-learn==1.6.1
openpyxl==3.1.5
python-multipart==0.0.12
sqlalchemy==2.0.36
openai==1.54.0
python-dotenv==1.0.1
pydantic==2.11.7
email-validator==2.2.0

# Supabase: Postgres driver + auth client. httpx is intentionally NOT pinned
# here — supabase-py and openai both depend on it, and letting pip resolve
# a mutually-compatible version avoids a repeat of the openai/httpx
# "proxies" kwarg incompatibility we hit earlier. llm_insights.py wraps the
# OpenAI client in a try/except specifically so any future version mismatch
# degrades to the rule-based insight generator instead of crashing requests.
supabase==2.31.0
psycopg2-binary==2.9.10
